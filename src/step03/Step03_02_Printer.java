package step03;

import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.StringTokenizer;

public class Step03_02_Printer {
    /**
     * 문제 설명
     * 프린터 대기열이 있습니다. 각 문서는 중요도가 있으며, 다음과 같은 규칙으로 인쇄됩니다:
     * 
     * 1. 대기열의 가장 앞에 있는 문서를 확인한다.
     * 2. 나머지 문서들 중 현재 문서보다 중요도가 높은 문서가 하나라도 있다면,
     *    이 문서를 인쇄하지 않고 대기열의 가장 뒤로 보낸다.
     * 3. 그렇지 않다면 바로 인쇄한다.
     * ================================================================
     * 현재 대기열에 있는 문서의 중요도가 순서대로 주어질 때,
     * 우리가 궁금한 문서가 몇 번째로 인쇄되는지 알아내는 프로그램을 작성하세요.
     * ================================================================
     * 제약 사항
     * 1. Java에서 제공하는 java.util.Queue 클래스를 사용하지 마세요.
     * 2. 배열을 활용하여 큐를 직접 구현해 사용하세요.
     * 3. 문서의 개수 N (1 <= N <= 100)
     * 4. 중요도는 1 이상 9 이하의 정수
     * ================================================================
     * 입력
     * 첫째 줄에 테스트 케이스의 수 T가 주어집니다.
     * 각 테스트 케이스는 두 줄로 이루어져 있습니다.
     * - 첫 번째 줄: 문서의 개수 N, 궁금한 문서의 위치 M (0부터 시작)
     * - 두 번째 줄: N개 문서의 중요도가 순서대로 주어짐
     * ================================================================
     * 출력
     * 각 테스트 케이스마다 궁금한 문서가 몇 번째로 인쇄되는지 출력합니다.
     * ================================================================
     * 예시 입력
     * 3
     * 1 0
     * 5
     * 4 2
     * 1 2 3 4
     * 6 0
     * 1 1 9 1 1 1
     * ================================================================
     * 예시 출력
     * 1
     * 2
     * 5
     * ================================================================
     * 설명
     * - 첫 번째 테스트: 문서가 1개뿐이므로 1번째로 인쇄
     * - 두 번째 테스트: [1(M), 2, 3, 4] → [2, 3, 4, 1] → [3, 4, 1] (2 인쇄)
     *                                    → [4, 1, 3] → [1, 3] (4 인쇄)
     *                                    → [3, 1] → [1] (3 인쇄, M 위치 2번째)
     * - 세 번째 테스트: 9가 가장 높은 중요도이므로 앞의 4개를 모두 뒤로 보내고 5번째로 인쇄
     */
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        int T = Integer.parseInt(br.readLine());
        
        for (int t = 0; t < T; t++) {
            StringTokenizer st = new StringTokenizer(br.readLine());
            int N = Integer.parseInt(st.nextToken());
            int M = Integer.parseInt(st.nextToken());
            
            st = new StringTokenizer(br.readLine());
            
            // 여기에 코드를 작성하세요
            
        }
    }
}
