package step02;

import java.io.BufferedReader;
import java.io.InputStreamReader;

public class Step02_07_Editor {
    /**
     * 문제 설명
     * 간단한 에디터를 구현하려고 합니다. 이 에디터는 커서를 기준으로 문자열을 편집합니다.
     * 처음에 편집기에 입력되어 있는 문자열이 주어지고, 그 이후 입력한 명령을 수행하면 됩니다.
     * 커서는 문자열의 맨 앞(첫 번째 문자의 왼쪽), 문자 사이, 맨 뒤(마지막 문자의 오른쪽) 중 하나에 위치할 수 있습니다.
     * ================================================================
     * 명령어
     * 1. L: 커서를 왼쪽으로 한 칸 옮김 (커서가 맨 앞이면 무시)
     * 2. D: 커서를 오른쪽으로 한 칸 옮김 (커서가 맨 뒤면 무시)
     * 3. B: 커서 왼쪽에 있는 문자를 삭제 (커서가 맨 앞이면 무시)
     * 4. P $: $라는 문자를 커서 왼쪽에 추가
     * ================================================================
     * 제약 사항
     * 1. 모든 명령어는 O(1) 시간복잡도로 동작해야 합니다.
     * 2. 초기 문자열의 길이는 N (1 <= N <= 100,000)
     * 3. 명령어의 개수는 M (1 <= M <= 500,000)
     * ================================================================
     * 입력
     * 첫째 줄에 초기 문자열이 주어집니다. (길이 N, 영어 소문자로만 이루어져 있음)
     * 둘째 줄에 명령어의 개수 M이 주어집니다.
     * 셋째 줄부터 M개의 줄에 명령어가 하나씩 주어집니다.
     * ================================================================
     * 출력
     * 모든 명령어를 수행하고 난 후 편집기에 입력되어 있는 문자열을 출력합니다.
     * ================================================================
     * 예시 입력
     * abcd
     * 3
     * P x
     * L
     * P y
     * ================================================================
     * 예시 출력
     * abcdyx
     * ================================================================
     * 설명
     * 초기: abcd|  (커서는 맨 뒤, |는 커서 위치)
     * P x: abcdx|
     * L:   abcd|x
     * P y: abcdy|x
     * 결과: abcdyx
     */
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String initialStr = br.readLine();
        int M = Integer.parseInt(br.readLine());

        // 여기에 코드를 작성하세요

    }
}
